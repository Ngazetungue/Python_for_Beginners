Few of list methods:

    list.sort()
    list.append(elem) -- This is a method we use to add a single element to the end of the list.
    list.reverse()
    list.insert(index, elem) -- This method used to inserts the element at the given index, shifting elements to the right.
    list.pop(index)
    list.index(elem)
    list.remove(elem)

Practical Example
Let's say you have a list of Country
Country  = ['India','Wale','Namibia','Angola', 'South Africa', 'Wales', 'Nigeria, 'Zimbabwe', 'Spain', 'Germany', 'New Zealand','Ghana']

#Lets append Australia to the list of countries 
Country.append('Australia')        #Take note Australia is added to the end of the list of country
